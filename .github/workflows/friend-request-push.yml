# Push social (pedidos de amizade + mensagens) — GRÁTIS (GitHub Actions + FCM, sem plano Blaze).
#
# Usa o mesmo secret FIREBASE_SERVICE_ACCOUNT do workflow "Daily charge push".
# Teste manual: Actions → Social push → Run workflow

name: Social push

on:
  schedule:
    - cron: '*/3 * * * *'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  send:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        working-directory: scripts
        run: npm install

      - name: Send friend request pushes
        working-directory: scripts
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: node send-friend-request-push.js

      - name: Send DM pushes
        working-directory: scripts
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: node send-dm-push.js
